% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/micros.R
\name{linCmtMicros}
\alias{linCmtMicros}
\title{Micro-constants of a linear compartment model}
\usage{
linCmtMicros(p1, v1, p2 = 0, p3 = 0, p4 = 0, p5 = 0, ncmt, trans = 1L)
}
\arguments{
\item{p1}{first parameter (for example clearance or \code{k})}

\item{v1}{central volume (or its parameterization-specific counterpart)}

\item{p2}{second parameter (two and three compartments)}

\item{p3}{third parameter (two and three compartments)}

\item{p4}{fourth parameter (three compartments)}

\item{p5}{fifth parameter (three compartments)}

\item{ncmt}{number of compartments, 1 to 3}

\item{trans}{parameterization number (see Details)}
}
\value{
named numeric vector: \code{v}, \code{k}, and for two or more compartments
\code{k12}, \code{k21}, and for three compartments \code{k13}, \code{k31}
}
\description{
Converts one parameter set, in any of the \code{linCmt()} parameterizations, to
the central volume and micro-constants that the analytic solutions use.
}
\details{
Supported \code{trans} values: one compartment 1 (\code{cl}, \code{v}), 2 (\code{k}, \code{v}), 10
(\code{alpha}, \code{A} with \code{v1} holding \code{A}), 11 (\code{alpha}, \code{v}); two compartments 1
(\code{cl}, \code{v}, \code{q}, \code{vp}), 2 (\code{k}, \code{v}, \code{k12}, \code{k21}), 3 (\code{cl}, \code{v}, \code{q},
\code{vss}), 4 (\code{alpha}, \code{beta}, \code{k21}), 5 (\code{alpha}, \code{beta}, \code{aob}), 10, 11;
three compartments 1 (\code{cl}, \code{v}, \code{q}, \code{vp}, \code{q2}, \code{vp2}), 2 (\code{k}, \code{v},
\code{k12}, \code{k21}, \code{k13}, \code{k31}), 10, 11.
}
\examples{
linCmtMicros(p1 = 2, v1 = 20, ncmt = 1)
linCmtMicros(p1 = 2, v1 = 20, p2 = 3, p3 = 40, ncmt = 2)
}
\author{
Matthew L. Fidler
}
