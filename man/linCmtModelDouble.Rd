% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{linCmtModelDouble}
\alias{linCmtModelDouble}
\title{Advance a linear compartment model by one step}
\usage{
linCmtModelDouble(
  dt,
  p1,
  v1,
  p2,
  p3,
  p4,
  p5,
  ka,
  alastNV,
  rateNV,
  ncmt,
  oral0,
  trans,
  deriv,
  type,
  tau,
  tinf,
  amt,
  bolusCmt,
  ndiff,
  sensType = 3L,
  sensH = 0.001
)
}
\arguments{
\item{dt}{time step}

\item{p1, v1, p2, p3, p4, p5}{parameters in the \code{trans} parameterization (see
\code{\link[=linCmtMicros]{linCmtMicros()}})}

\item{ka}{absorption rate constant (used when \code{oral0} is 1)}

\item{alastNV}{amounts at the start of the step (depot first when
\code{oral0} is 1, then central and peripherals); with \code{deriv = TRUE}
followed by their parameter sensitivities}

\item{rateNV}{zero-order rates into each compartment, in the same order}

\item{ncmt}{number of compartments, 1 to 3}

\item{oral0}{1 when the model has a depot compartment, otherwise 0}

\item{trans}{parameterization number}

\item{deriv}{logical; also return the derivatives}

\item{type}{0 for a regular step; 1 and 2 steady-state infusion, 3
steady-state bolus}

\item{tau}{steady-state dosing interval}

\item{tinf}{steady-state infusion duration}

\item{amt}{steady-state bolus amount}

\item{bolusCmt}{steady-state bolus compartment}

\item{ndiff}{differentiation bit mask as used by rxode2 (\code{ka} 1, \code{p1} 2,
\code{v1} 4, \code{p2} 8, \code{p3} 16, \code{p4} 32, \code{p5} 64)}

\item{sensType}{derivative method: 3 or 30 forward-mode automatic
differentiation, 32 all directions in one forward-mode pass, 31
reverse mode, 100 automatic choice, 1 and 2 forward and central
differences with the kernel's own step, 10 and 20 forward and central
differences with step \code{sensH}}

\item{sensH}{finite-difference step for \code{sensType} 10 and 20}
}
\value{
list with \code{val} (central concentration) and \code{Alast} (carried
state after the step); with \code{deriv = TRUE} also \code{J} (Jacobian of the
amounts) and \code{Jg} (gradient of \code{val})
}
\description{
Advances the amounts of a one, two or three compartment linear model by
\code{dt} and returns the central concentration, optionally with its
derivatives.  This is the per-row kernel behind rxode2's \code{linCmt()}.
}
\examples{
linCmtModelDouble(1, 2, 20, 0, 0, 0, 0, 0, 100, 0, 1L, 0L, 1L, FALSE,
                  0L, 0, 0, 0, 0L, 0L)
}
