% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{.linCmtCarryLiveTest}
\alias{.linCmtCarryLiveTest}
\title{Drive linCmtB() carry sentinels on a solved subject (test hook)}
\usage{
.linCmtCarryLiveTest(
  id,
  t,
  tPrior,
  theta,
  ncmt,
  oral0,
  trans,
  which1,
  which2,
  addVal = NULL
)
}
\arguments{
\item{id}{0-based subject index in the most recent solve}

\item{t}{output time of each row}

\item{tPrior}{time of the preceding row (0 for the first row)}

\item{theta}{numeric matrix with one row per element of \code{t} and 7
columns: p1, v1, p2, p3, p4, p5, ka}

\item{ncmt}{number of compartments, 1 to 3}

\item{oral0}{1 when the model has a depot compartment, otherwise 0}

\item{trans}{parameterization number}

\item{which1}{per-row \code{which1} argument passed to linCmtB()}

\item{which2}{per-row \code{which2} argument passed to linCmtB()}

\item{addVal}{for rows with \code{which1 = -7}, the value to add (passed in
the p2 argument); \code{NULL} for none}
}
\value{
numeric vector of linCmtB() results, one per row
}
\description{
Replays linCmtB() calls for one subject of the most recent rxode2 solve
in the same session, the way that subject's output pass would, so tests
can check the carry sentinels.  Needs rxode2 to be loaded and a solve to
have run.
}
\keyword{internal}
