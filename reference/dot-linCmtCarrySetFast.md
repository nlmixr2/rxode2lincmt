# Toggle the linCmt() carry-advance runtime fast path (test/benchmark hook)

Toggle the linCmt() carry-advance runtime fast path (test/benchmark
hook)

## Usage

``` r
.linCmtCarrySetFast(enable)
```

## Arguments

- enable:

  logical; new state

## Value

the previous state, invisibly
